'use strict'

const Note = require('../model/other')
const otherCtrl = require('../controller/other-controller')

module.exports = function(router) {
  router.post('/api/other', (req, res) => {

  })

  router.get('/api/other/:id', (req, res) => {

  })
}
