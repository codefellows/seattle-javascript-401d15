'use strict'

const Note = require('../model/some')
const someCtrl = require('../controller/some-controller')

module.exports = function(router) {
  router.post('/api/some', (req, res) => {

  })

  router.get('/api/some/:id', (req, res) => {

  })
}
